[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="codewatch"
version="0.1.0"
description="Simple text snapshot change tracking"
requires-python=">=3.9"
license={text="MIT"}
[tool.pytest.ini_options]
pythonpath=["src"]
